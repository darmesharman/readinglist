spring:
  datasource:
    url: jdbc:mysql://localhost:3307/readinglist
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: true

amazon:
  associate-id: habuma-20
